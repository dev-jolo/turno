# CLAUDE.md

## Agent skills

### Issue tracker

Local markdown under `.scratch/` (gitignored, not committed). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` at the repo root, created lazily as terms/decisions get resolved. See `docs/agents/domain.md`.
